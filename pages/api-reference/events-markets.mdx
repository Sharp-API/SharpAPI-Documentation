import { Callout, Tabs } from 'nextra/components'

# Event Markets

Get the available market types for a specific event, including how many sportsbooks offer each market.

```
GET /api/v1/events/{eventId}/markets
```

## Authentication

Requires API key via `X-API-Key` header, `Authorization: Bearer` header, or `api_key` query param. Available to all tiers.

## Path Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `eventId` | string | Yes | Unique event identifier (e.g., `evt_nba_bos_lal_20260208`) |

## Response Headers

All responses include standard rate limit and metadata headers:

| Header | Description |
|--------|-------------|
| `X-RateLimit-Limit` | Maximum requests per minute for your tier |
| `X-RateLimit-Remaining` | Requests remaining in current window |
| `X-RateLimit-Reset` | Unix timestamp when the rate limit window resets |
| `X-Tier` | Your current subscription tier |
| `X-Data-Delay` | Data delay for your tier (e.g., `0s`, `60s`) |
| `X-Request-Id` | Unique request identifier for debugging |

## Market Object

| Field | Type | Description |
|-------|------|-------------|
| `market_type` | string | Market type identifier (e.g., `moneyline`, `spread`, `total`) |
| `display_name` | string | Human-readable market name |
| `odds_count` | integer | Total number of odds lines for this market |
| `sportsbook_count` | integer | Number of sportsbooks offering this market |
| `sportsbooks` | array | List of sportsbook identifiers offering this market |

## Example Requests

<Tabs items={['cURL', 'JavaScript', 'Python']}>
  <Tabs.Tab>
```bash
curl -X GET "https://api.sharpapi.io/api/v1/events/evt_nba_bos_lal_20260208/markets" \
  -H "X-API-Key: YOUR_API_KEY"
```
  </Tabs.Tab>
  <Tabs.Tab>
```javascript
const eventId = 'evt_nba_bos_lal_20260208';
const response = await fetch(
  `https://api.sharpapi.io/api/v1/events/${eventId}/markets`,
  { headers: { 'X-API-Key': 'YOUR_API_KEY' } }
);
const { data, meta } = await response.json();

// Find markets available on all books
for (const market of data) {
  console.log(`${market.display_name}: ${market.sportsbook_count} books, ${market.odds_count} odds`);
}
```
  </Tabs.Tab>
  <Tabs.Tab>
```python
import requests

event_id = 'evt_nba_bos_lal_20260208'
response = requests.get(
    f'https://api.sharpapi.io/api/v1/events/{event_id}/markets',
    headers={'X-API-Key': 'YOUR_API_KEY'}
)
result = response.json()

for market in result['data']:
    books = ', '.join(market['sportsbooks'])
    print(f"{market['display_name']}: {market['sportsbook_count']} books ({books})")
```
  </Tabs.Tab>
</Tabs>

## Response

### Success (200)

```json
{
  "data": [
    {
      "market_type": "moneyline",
      "display_name": "Moneyline",
      "odds_count": 12,
      "sportsbook_count": 6,
      "sportsbooks": [
        "draftkings",
        "fanduel",
        "betmgm",
        "caesars",
        "bet365",
        "pinnacle"
      ]
    },
    {
      "market_type": "spread",
      "display_name": "Spread",
      "odds_count": 12,
      "sportsbook_count": 6,
      "sportsbooks": [
        "draftkings",
        "fanduel",
        "betmgm",
        "caesars",
        "bet365",
        "pinnacle"
      ]
    },
    {
      "market_type": "total",
      "display_name": "Total (Over/Under)",
      "odds_count": 12,
      "sportsbook_count": 6,
      "sportsbooks": [
        "draftkings",
        "fanduel",
        "betmgm",
        "caesars",
        "bet365",
        "pinnacle"
      ]
    },
    {
      "market_type": "player_prop",
      "display_name": "Player Props",
      "odds_count": 156,
      "sportsbook_count": 4,
      "sportsbooks": [
        "draftkings",
        "fanduel",
        "betmgm",
        "caesars"
      ]
    },
    {
      "market_type": "team_total",
      "display_name": "Team Total",
      "odds_count": 8,
      "sportsbook_count": 4,
      "sportsbooks": [
        "draftkings",
        "fanduel",
        "betmgm",
        "pinnacle"
      ]
    },
    {
      "market_type": "first_half_spread",
      "display_name": "1st Half Spread",
      "odds_count": 6,
      "sportsbook_count": 3,
      "sportsbooks": [
        "draftkings",
        "fanduel",
        "betmgm"
      ]
    }
  ],
  "meta": {
    "count": 6,
    "total": 6,
    "pagination": {
      "limit": 50,
      "offset": 0,
      "has_more": false,
      "next_offset": null
    },
    "updated_at": "2026-02-08T12:05:00Z",
    "filters": {}
  }
}
```

### Error Responses

```json title="404 Not Found"
{
  "error": {
    "code": "not_found",
    "message": "Event not found",
    "docs": "https://sharpapi.io/docs/api-reference/events-markets"
  }
}
```

```json title="401 Unauthorized"
{
  "error": {
    "code": "unauthorized",
    "message": "Missing or invalid API key",
    "docs": "https://sharpapi.io/docs/authentication"
  }
}
```

## Use Cases

- **Market discovery** -- Determine which market types are available before requesting odds, reducing unnecessary API calls
- **Sportsbook coverage** -- See which sportsbooks cover each market to identify comparison opportunities
- **Line shopping prep** -- Identify markets offered by multiple books where you can shop for the best line
- **Player props availability** -- Check if player prop markets are available (often limited to certain books and closer to game time)

<Callout type="info">
Use this endpoint to check market availability before calling [Event Odds](/api-reference/events-odds) with a `market` filter. This avoids wasting API calls on market types that do not exist for a given event.
</Callout>

## Related Endpoints

- [Events](/api-reference/events) - List and search events
- [Event Details](/api-reference/events-by-id) - Get event details with embedded market data
- [Event Odds](/api-reference/events-odds) - Get all odds for this event
- [Markets](/api-reference/markets) - List all market types across the platform
